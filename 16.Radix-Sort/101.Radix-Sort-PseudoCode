PseudoCode for Radix Sort Function

- Define a function that accepts a list of numbers
- Figure out how many digits the largest number has.
- Loop from l =0 up to this largest number of digits.
- For each iteration of the loop:
    - Create buckets each digit (0-9).
    - Place each number in the corresponding bucket based on it's k^th digit.
- Replace our existing array with value in our buckets, startign with 0 and going up to 9.
- Return list at the end. 



